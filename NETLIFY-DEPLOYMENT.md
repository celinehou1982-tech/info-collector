# Netlify 部署指南

项目已经成功构建，现在可以部署到 Netlify 了！

## 方法一：拖拽部署（最简单，推荐）

### 步骤：

1. **访问 Netlify**
   - 打开 https://app.netlify.com
   - 注册/登录账号（可以使用 GitHub 账号快速登录）

2. **拖拽上传**
   - 在 Netlify 首页找到 "Want to deploy a new site without connecting to Git?"
   - 将整个 `dist` 文件夹拖拽到上传区域
   - 路径：`/Users/houjie5/info-collector/frontend/dist`

3. **等待部署完成**
   - Netlify 会自动部署并生成一个随机域名
   - 例如：`https://random-name-123456.netlify.app`

4. **访问你的应用**
   - 使用手机浏览器访问生成的 URL
   - 完成！

### 重新部署更新：

如果代码有更新：
```bash
cd /Users/houjie5/info-collector/frontend
npm run build
```
然后再次拖拽 `dist` 文件夹到 Netlify 网站，会自动更新。

---

## 方法二：使用 Netlify CLI（自动化）

### 1. 安装 Netlify CLI

```bash
npm install -g netlify-cli
```

### 2. 登录 Netlify

```bash
netlify login
```
会自动打开浏览器进行授权。

### 3. 初始化并部署

在项目前端目录中：

```bash
cd /Users/houjie5/info-collector/frontend
netlify deploy
```

首次部署会询问：
- **Create & configure a new site**: 选择这个
- **Team**: 选择你的团队（通常是你的用户名）
- **Site name**: 留空会自动生成，或者输入自定义名称
- **Publish directory**: 输入 `dist`

### 4. 测试部署

上面的命令会创建一个 draft URL 用于测试。测试没问题后，运行：

```bash
netlify deploy --prod
```

这会将应用部署到生产环境。

### 5. 后续更新

每次代码更新后：

```bash
npm run build
netlify deploy --prod
```

---

## 方法三：连接 GitHub 自动部署（推荐用于持续开发）

### 1. 将代码推送到 GitHub

如果还没有 Git 仓库：

```bash
cd /Users/houjie5/info-collector
git init
git add .
git commit -m "Initial commit"

# 在 GitHub 上创建仓库后
git remote add origin https://github.com/你的用户名/info-collector.git
git push -u origin main
```

### 2. 在 Netlify 连接 GitHub

1. 访问 https://app.netlify.com
2. 点击 "Add new site" → "Import an existing project"
3. 选择 "GitHub"
4. 授权 Netlify 访问你的仓库
5. 选择 `info-collector` 仓库

### 3. 配置构建设置

Netlify 会自动识别 `netlify.toml` 配置文件：

```toml
[build]
  command = "npm run build"
  publish = "dist"
  base = "frontend"
```

如果需要手动配置：
- **Base directory**: `frontend`
- **Build command**: `npm run build`
- **Publish directory**: `frontend/dist`

### 4. 部署

点击 "Deploy site" 按钮，Netlify 会：
- 自动拉取代码
- 安装依赖
- 运行构建
- 部署到 CDN

### 5. 自动部署

以后每次推送到 GitHub main 分支，Netlify 会自动重新部署！

```bash
git add .
git commit -m "Update feature"
git push
```

---

## 配置自定义域名（可选）

### 使用 Netlify 子域名：

1. 进入站点设置 → Domain settings
2. 点击 "Options" → "Edit site name"
3. 修改为你想要的名称，例如 `my-info-collector`
4. 域名变为：`https://my-info-collector.netlify.app`

### 使用自己的域名：

1. 进入站点设置 → Domain settings
2. 点击 "Add custom domain"
3. 输入你的域名（需要你已经购买）
4. 按照指引在域名提供商处添加 DNS 记录

---

## 环境变量配置（如果需要）

如果你的应用需要环境变量（如 API keys）：

1. 进入站点设置 → Environment variables
2. 添加变量，例如：
   - `VITE_API_URL`: 后端 API 地址
   - `VITE_API_KEY`: API 密钥

然后在代码中使用：
```typescript
const apiUrl = import.meta.env.VITE_API_URL
```

---

## 性能优化

### 启用 Brotli 压缩

Netlify 自动启用 gzip 和 Brotli 压缩，无需配置。

### 配置缓存

已经在 `netlify.toml` 中配置：
- 静态资源（CSS/JS）缓存 1 年
- Service Worker 和 manifest.json 不缓存
- HTML 不缓存（确保用户总是获取最新版本）

### CDN 分发

Netlify 自动将你的应用部署到全球 CDN，访问速度很快！

---

## 常见问题

### Q: 刷新页面 404？
A: 已经在 `netlify.toml` 中配置了 SPA 重定向规则，应该不会出现这个问题。

### Q: 构建失败？
A: 检查 Netlify 构建日志，确保：
- Node.js 版本正确（我们设置为 18）
- 依赖安装成功
- 构建命令正确

### Q: 应用很慢？
A: Netlify 免费版在亚洲地区可能较慢，考虑：
- 升级到付费版
- 或使用 Vercel（对亚洲更友好）
- 或使用中国的托管服务（如阿里云OSS + CDN）

### Q: 如何查看部署状态？
A: 在 Netlify 控制台的 "Deploys" 标签可以查看所有部署记录和日志。

### Q: 如何回滚到之前的版本？
A: 在 "Deploys" 标签中，找到之前成功的部署，点击 "Publish deploy"。

---

## 快速开始总结

**最快的方式（5分钟完成）：**

1. 打开终端，构建项目：
   ```bash
   cd /Users/houjie5/info-collector/frontend
   npm run build
   ```

2. 打开 https://app.netlify.com

3. 将 `/Users/houjie5/info-collector/frontend/dist` 文件夹拖拽到网页上

4. 完成！复制生成的 URL 在手机上访问

---

## 部署后的 URL

部署成功后，你会得到类似这样的 URL：
```
https://your-site-name.netlify.app
```

这个 URL 可以在任何设备上访问，包括：
- 手机浏览器
- 平板
- 其他电脑
- 可以分享给其他人

**注意**：
- 免费版 Netlify 有一些限制（带宽、构建时间等），但对个人使用足够了
- 如果需要更多功能，可以考虑升级或使用其他平台

祝部署顺利！🎉
