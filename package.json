{
  "name": "info-collector-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "postbuild": "cp -r frontend/dist ./dist"
  }
}
